# Client side LLM example

- lets explore the various client side (js) LLM's available 
- build a simple html demo with a tiny llm (gemini already embeds their llm in chrome?)
- let the user chat with the client side llm 